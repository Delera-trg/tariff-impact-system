# 网络请求（保留常用版本）
urllib3>=1.26.18
requests>=2.31.0

# Streamlit 核心（去掉固定版本号，避免环境冲突）
streamlit>=1.23.1

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 数据库（只保留标准SQLAlchemy，去掉sqlite-utils）
sqlalchemy>=2.0.0

# Excel导出
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# 数据验证
pydantic>=2.0.0

# 工具
python-dotenv>=1.0.0
pyyaml>=6.0.0